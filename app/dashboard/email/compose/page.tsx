"use client";
import { useState } from "react";
export default function ComposeEmailPage(){const [subject,setSubject]=useState("");const [body,setBody]=useState("");const [status,setStatus]=useState("");async function send(){setStatus("Sending…");const r=await fetch("/api/email/send",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({subject,body,audience:"all"})});setStatus(r.ok?"Queued for eligible subscribers.":"Unable to send. Check server configuration.");}return <main className="min-h-screen bg-slate-950 px-5 py-8 text-white"><div className="mx-auto max-w-3xl"><h1 className="text-3xl font-semibold">Compose email</h1><p className="mt-2 text-sm text-slate-400">Only eligible, non-suppressed subscribers can receive outbound messages.</p><div className="mt-8 space-y-5"><label className="block text-sm">Subject<input value={subject} onChange={e=>setSubject(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[0.04] p-3"/></label><label className="block text-sm">Message<textarea value={body} onChange={e=>setBody(e.target.value)} rows={12} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[0.04] p-3"/></label><button onClick={send} disabled={!subject||!body} className="rounded-xl bg-white px-5 py-3 text-sm font-semibold text-slate-950 disabled:opacity-40">Send to eligible subscribers</button>{status&&<p className="text-sm text-cyan-300">{status}</p>}</div></div></main>}
